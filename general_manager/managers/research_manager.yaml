name: research_manager
version: "1.0"

identity:
  system_prompt: "../prompts/research_manager.md"  # relative to this file's location

agency:
  provider: openai
  model: gpt-4o
  temperature: 0.7

tools:
  - brave_search
  - filesystem

runtime:
  max_iterations: 10
  timeout_seconds: 300

terminal:
  enabled: true
  prompt_style: minimal
